server:
  port: 8082

spring:
  mail:
    host: smtp.gmail.com
    username: hirexhire@gmail.com
    password: cregqlcluawsnwhv
    port: 587
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: email-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: com.hpms.commonlibs.dto
        spring.json.use.type.headers: false
        spring.json.value.default.type: com.hpms.commonlib.dto.EmailEvent

email:
  from: hirexhire@gmail.com
  from-name: HirexHire

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true